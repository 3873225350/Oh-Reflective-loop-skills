\section{Appendix}

\subsection{Supplementary Visual Overview}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/xyz-agent-reflective-loop-skills-framework.png}
\caption{A broader synthesis of \ours{} with a three-axis collaboration view. The figure combines parallel role decomposition, temporal reflective evolution, and heterogeneous agent capabilities under one shared task objective.}
\label{fig:xyz_framework}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/long-task-loop-selection-guide.png}
\caption{Compact selection guide for long-task loop mechanisms. Hook loops are best when preventing false completion is the priority, daemon loops favor unattended background progress, and application cron loops suit product-managed periodic tasks.}
\label{fig:loop_selection}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/hook-loop-trigger-boundary.png}
\caption{Supplementary breakdown of the hook-loop trigger boundary. The continuation decision is made when the runtime is about to stop, allowing the system to block premature completion and refeed a reason into the next step.}
\label{fig:hook_detail}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/external-daemon-loop-trigger-boundary.png}
\caption{Supplementary breakdown of the external daemon loop. A detached process advances long tasks through periodic wake-ups, bounded prompts, heartbeat logging, and resumable state files.}
\label{fig:daemon_detail}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/application-cron-loop-trigger-boundary.png}
\caption{Supplementary breakdown of the application cron loop. The scheduler lives inside the application process, creates scheduled tasks, and enqueues prompts when the next firing time is reached.}
\label{fig:cron_detail}
\end{figure*}

\subsection{Repository Layout}

Each primary loop skill follows the same layout:
\begin{verbatim}
<name>-loop/
  README.md
  SKILL.md
  prompt.md
  scripts/
    init_<name>_loop.cjs
    run_daemon.py
    run_daemon.sh
    start_<name>_loop.sh
    stop_<name>_loop.sh
    status_<name>_loop.sh
    print_cron_entry.sh
\end{verbatim}

\subsection{Example Active Task Schema}

\begin{verbatim}
{
  "plan_id": "PROJECT_ROADMAP",
  "mode": "optimize",
  "bounded_target": "complete one verifiable slice",
  "prompt_patch": [],
  "scope_patch": [],
  "verification_patch": [],
  "handoff": "next smallest useful action"
}
\end{verbatim}

\subsection{Example Failure Memory Entry}

\begin{verbatim}
{
  "failure_type": "verification_gap",
  "symptom": "implementation completed without running target tests",
  "patch": "before final handoff, run the smallest relevant check",
  "seen_in": ["epoch-004", "epoch-009"]
}
\end{verbatim}

\subsection{Daemon Management Commands}

All loop skills share the same management interface:

\begin{verbatim}
# Initialize state
node <skill>-loop/scripts/init_<name>_loop.cjs MY_ROADMAP

# Start daemon (tmux/nohup auto-select)
bash <skill>-loop/scripts/start_<name>_loop.sh MY_ROADMAP

# Check status (PID health + last mode)
bash <skill>-loop/scripts/status_<name>_loop.sh MY_ROADMAP

# Stop daemon gracefully (SIGTERM -> SIGKILL)
bash <skill>-loop/scripts/stop_<name>_loop.sh MY_ROADMAP

# Print cron entries for OS-level supervision
bash <skill>-loop/scripts/print_cron_entry.sh
\end{verbatim}

\subsection{Cron Integration Example}

The following crontab entries provide automatic restart after reboot and periodic health checks:
\begin{verbatim}
# Auto-start after reboot
@reboot cd /workspace && <SKILL>_LOOP_WORKSPACE=/workspace \
  bash /path/to/start_<name>_loop.sh

# Health check every N minutes (restart on failure)
*/N * * * * cd /workspace && \
  bash /path/to/status_<name>_loop.sh \
  || bash /path/to/start_<name>_loop.sh
\end{verbatim}

\subsection{Implementation Notes}

The Codex and MOA loops include additional monitoring commands:
\begin{verbatim}
# Codex: real-time monitoring dashboard
bash codex-loop/scripts/monitor_codex_loop.sh --watch

# MOA: DAG + Blackboard + PEFT dashboard
bash moa-loop/scripts/monitor_moa_loop.sh --watch
\end{verbatim}

The MOA loop extends the abstraction with \texttt{dag\_scheduler.py}, \texttt{shared\_blackboard.py}, and \texttt{iteration\_manager.py}. These modules implement horizontal DAG scheduling, centralized agent communication, and epoch-level PEFT adaptation tracking.
